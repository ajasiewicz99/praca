framework:
    messenger:
        # Uncomment this (and the failed transport below) to send failed messages to this transport for later handling.
        # failure_transport: failed

        transports:
            # https://symfony.com/doc/current/messenger.html#transport-configuration
            async: '%env(MESSENGER_TRANSPORT_DSN)%'
            failed: 'doctrine://default?queue_name=failed'
            sync: 'sync://'
        default_bus: command.bus
        buses:
            command.bus: ~
            query.bus: ~

        routing:
            'App\Module\User\Infrastructure\Messenger\SendEmailMessage': async
            'App\Module\User\Infrastructure\Messenger\RegisterUserCommand': async
#            'App\Messenger\AddProductToCart': async
#            'App\Messenger\RemoveProductFromCart': async
#            'App\Messenger\AddProductToCatalog': async
#            'App\Messenger\RemoveProductFromCatalog': async
